{
  "name": "movie-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server" : "json-server --watch movie.json --port 5555"
  },
  "devDependencies": {
    "vite": "^2.9.9"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
